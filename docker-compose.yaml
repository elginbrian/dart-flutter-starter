version: "3.8"

services:
  flutter-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:80"
    volumes:
      - .:/app
    networks:
      - flutter-network
    restart: always

networks:
  flutter-network:
    driver: bridge
